import { MapaClientPage } from "./MapaClientPage";

/** Aba `mapa` — o artboard `9a`. A pagina e fina; a logica vive no cliente,
 *  porque a prova-alvo depende do token do aluno. */
export default function MapaPage() {
  return <MapaClientPage />;
}
