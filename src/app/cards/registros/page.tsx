import CadernoClientPage from "./CadernoClientPage";

/** Mesma coisa de `/cards`: quem barra e o `redirects()` do `next.config.js`,
 *  nao esta pagina. Ver o aviso la. */
export default function Page() {
  return <CadernoClientPage />;
}
