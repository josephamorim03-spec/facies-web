import { InicioDashboard } from "./_components/InicioDashboard";

/** `/inicio` — a primeira aba. Ver o docstring do dashboard para o desenho. */
export default function InicioPage() {
  return <InicioDashboard />;
}
